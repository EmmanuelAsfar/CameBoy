# Logs d'exécution (exclure)
logs/emulator.log
logs/emulator_gui.log
logs/last_run.txt
logs/last_*.txt
logs/rom/

# Logs de tests (inclure)
!logs/test_results.log
!logs/test_build.log

# Build outputs (keep resources under build/resources)
build/**
!build/.gitkeep
!build/resources/
!build/resources/**

# OS/editor files
.DS_Store
Thumbs.db

# Keep ROMs tracked (do NOT ignore .gb)
# (intentionally no pattern for *.gb)
